import React from 'react'

describe('InnloggetSide', () => {
  it('skal ikke vise skjema dersom man ikke har rettigheter', () => {
  })

  it('skal vise link til MinSideArbeidsgiver', () => {
  })

  it('skal vise bedriftsvelger dersom man har rettigheter', () => {
  })
  it('skal vise under elementer dersom man har rettigheter', () => {
  })
  it('skal vise TimeoutAdvarsel dersom man har rettigheter', () => {
  })

  it('skal vise advarsel dersom man ikke har rettigheter', () => {
  })
  it('skal ikke vise under elementer dersom man ikke har rettigheter', () => {
  })
  it('skal ikke vise TimeoutAdvarsel dersom man ikke har rettigheter', () => {
  })
});
